# Build output — all CMake preset build dirs
build/
install/

# Doxygen generated output — rebuilt with: cmake --build --preset <preset> --target docs
docs/doxygen/

# Compiled shaders — source is committed, binaries are not
shaders/*.spv

# vcpkg installed packages — reproduced from vcpkg.json on configure
vcpkg_installed/

# Obsidian vault config — local plugin/theme state, not project content
docs/FYP-Vault/.obsidian/

# CLion
.idea/

# Visual Studio
.vs/
*.user
*.suo

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# compile_commands.json (optional — some prefer to commit this for clangd)
# compile_commands.json
